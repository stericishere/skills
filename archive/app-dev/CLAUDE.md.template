# Global CLAUDE.md — Solo Founder Configuration

> Copy this to `~/.claude/CLAUDE.md` and customize per your needs.
> For project-specific overrides, place a `CLAUDE.md` in the project root.

## Identity

Solo founder building SaaS products. I operate across the full product lifecycle:
ideate → design → build → launch → iterate → loop.

## Tech Stack

### Web
- Framework: Next.js (App Router)
- Language: TypeScript (strict mode, no `any`)
- Styling: Tailwind CSS
- Components: shadcn/ui
- State: React hooks + Zustand (when needed)
- Auth: Supabase Auth
- Database: Supabase (PostgreSQL)
- Payments: Stripe
- Hosting: Vercel

### Mobile
- Framework: Expo (React Native) or Swift/SwiftUI (iOS native)
- Navigation: Expo Router or SwiftUI NavigationStack
- State: React hooks / SwiftUI @State + @Observable

### Shared Conventions
- Functional components only, hooks only
- Error boundaries on all route segments
- Explicit over clever
- Minimal diff: achieve goals with fewest new abstractions
- DRY is important — flag repetition aggressively
- Well-tested code is non-negotiable
- ASCII diagrams in code comments for complex flows

## Coding Standards

### TypeScript
- `strict: true` in tsconfig
- No `any` — use `unknown` with type guards
- Prefer `interface` over `type` for object shapes
- Use discriminated unions for state machines
- Barrel exports from feature directories

### React / Next.js
- Server Components by default, `'use client'` only when needed
- Colocate components with their routes
- Use `loading.tsx` and `error.tsx` at every route segment
- Prefer Server Actions over API routes for mutations
- Image optimization via `next/image`

### Testing
- Unit: Vitest
- E2E: Playwright (web), iOS Simulator (mobile)
- Coverage target: 80%+ on business logic
- Test naming: `describe('ComponentName', () => { it('should do X when Y') })`

### Git
- Conventional commits: `feat:`, `fix:`, `chore:`, `refactor:`, `docs:`, `test:`
- PR size: <500 LOC preferred, flag >1000 LOC
- Bisectable commits: each commit = one logical change

## Workflow — App Development SOP

This project follows a 5-phase lifecycle. Each phase is a skill: `/phase-1-ideate` through `/phase-5-iterate`.

**Before starting any phase:** Read the upstream `docs/<phase>/claude.md` files.
**After completing any phase:** Write the phase journal to `docs/<phase>/claude.md`.

### Phase Quick Reference

| Command | Phase | What It Does |
|---------|-------|-------------|
| `/office-hours` | 1. Ideate | YC-style forcing questions before research |
| `/browse` | 1. Ideate | Reddit + App Store gap research |
| `/plan-ceo-review` | 1. Ideate | CEO-level scope review (EXPANSION/HOLD/REDUCTION) |
| `/design-consultation` | 2. Design | Design system + design prompt generation |
| `/plan-eng-review` | 2. Design | Engineering architecture review |
| `architecture-decision-records` | 2. Design | Document ADRs |
| `/plan-ceo-review` | 3. Build | Feature list CEO review |
| `pm/user-story` | 3. Build | Write user stories (Cohn + Gherkin) |
| `pm/prioritization-advisor` | 3. Build | Score and rank features |
| `superpowers:writing-plans` | 3. Build | Decompose features into implementation plan |
| `superpowers:executing-plans` | 3. Build | Parallel agent execution |
| `/qa` | 3. Build | QA testing via headless browser |
| `/design-review` | 3. Build | Visual QA audit |
| `/simplify` | 3. Build | Complexity reduction |
| `/review` | 3. Build | Structured code review |
| `/codex` | 3. Build | Cross-model code review |
| `marketing/launch-strategy` | 4. Launch | 5-phase launch planning |
| `marketing/copywriting` | 4. Launch | Competitive copy generation |
| `marketing/analytics-tracking` | 4. Launch | Analytics setup |
| `marketing/ab-test-setup` | 4. Launch | A/B test design |
| `/ship` | 4. Launch | Automated ship workflow |
| `pm/prioritization-advisor` | 5. Iterate | RICE scoring for feature requests |
| `/retro` | 5. Iterate | Engineering retrospective |

## Engineering Preferences

These guide ALL plan reviews and code reviews:

1. **DRY** — flag repetition aggressively
2. **Well-tested** — too many tests > too few tests
3. **Engineered enough** — not fragile, not over-abstracted
4. **More edge cases** — thoughtfulness > speed
5. **Explicit > clever** — readable code wins
6. **Minimal diff** — fewest new abstractions and files touched
7. **Observability is scope** — logs, metrics, traces for new codepaths
8. **Security is scope** — threat model for new codepaths
9. **Diagrams are mandatory** — ASCII art for complex flows, in code comments
10. **Zero silent failures** — every failure mode must be visible

## Project Docs Structure

Every project maintains a `docs/` folder with accumulated phase decisions:

```
docs/
├── ideate/claude.md
├── design/claude.md + adr/
├── build/claude.md + user-stories/ + feature-specs/
├── launch/claude.md + memory/ (checkpoints, decisions, learnings)
└── iterate/claude.md + memory/ (decisions, learnings)
```

**Rule:** Always read upstream phase journals before starting work. Always write your phase journal when done.

**Memory:** Phase 4 and 5 have project-local `memory/` directories for cross-session persistence. These are git-tracked and project-specific.
